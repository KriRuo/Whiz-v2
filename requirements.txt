# Whiz dependencies
# Install with: pip install -r requirements.txt
#
# torch is intentionally excluded — it is only used for CUDA detection,
# which is optional. The app runs on CPU via ctranslate2 without it.

PyQt5==5.15.10            # GUI framework
faster-whisper>=1.0.3     # Speech-to-text engine (wraps ctranslate2)
sounddevice>=0.4.7        # Audio capture
pynput>=1.7.7             # Global hotkey support
pyautogui>=0.9.54         # Auto-paste to active window
numpy>=1.24.0,<2.0        # Audio processing
psutil>=5.9.8             # Process/system utilities

# Windows-only
pywin32>=306; sys_platform == 'win32'  # Single-instance management, tray icon
